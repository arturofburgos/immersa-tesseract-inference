# Tesseract configuration file
# Generated by tesseract 1.11.0 on 2026-08-22T07:24:18.648539
# Modified to package the final frozen V3 WakeSurrogate model.

name: "immersa_tesseract_inference_wake_surrogate"
version: "0.1.0"
description: >-
  Differentiable neural surrogate for sparse wake velocity measurements
  behind a fixed flat plate. Maps angle of attack and arbitrary sensor
  coordinates to ux and uy at five fixed observation times.

metadata:
  model: "WakeSurrogate V3"
  model_variant: "broad_aoa_refinement"
  reynolds_number: 200
  observation_times: [12.0, 13.3, 15.1, 17.4, 20.0]
  surrogate_inputs:
    - "angle_of_attack_deg"
    - "x"
    - "y"
  surrogate_outputs:
    - "ux"
    - "uy"

build_config:
  target_platform: "native"

  package_data:
    - [
        "../../../models/wake_surrogate/v3_broad_aoa_refinement/best_model.eqx",
        "best_model.eqx"
      ]
    - [
        "../../../models/wake_surrogate/v3_broad_aoa_refinement/normalization.npz",
        "normalization.npz"
      ]