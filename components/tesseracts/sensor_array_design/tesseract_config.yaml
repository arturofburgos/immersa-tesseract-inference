# Tesseract configuration file
# Generated by tesseract 1.11.0 on 2026-08-23T20:27:40.707250

name: "immersa_tesseract_inference_sensor_array_design"
version: "0.1.0"
description: >-
  Global angle-of-attack discriminability of a sparse wake measurement batch.
  Consumes predicted measurements over a design AoA grid and returns a single
  differentiable score: the soft minimum over pairwise measurement distances.
  Sensor coordinates are deliberately not an input; spatial bounds and sensor
  separation are design-variable constraints handled by the application.

metadata:
  criterion: "soft-minimum pairwise AoA discriminability"
  observation_times: [12.0, 13.3, 15.1, 17.4, 20.0]
  differentiable_inputs:
    - "measurements"
  differentiable_outputs:
    - "discrimination"

build_config:
  target_platform: "native"
