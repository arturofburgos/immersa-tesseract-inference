# Tesseract requirements file
# Generated by tesseract 1.11.0 on 2026-08-05T22:43:17.254229

# Frozen to the validated WakeObservation runtime, matching WakeSurrogate so
# both JAX components resolve identically. Equinox is a real dependency here:
# apply_jit is wrapped with @eqx.filter_jit.
jax[cpu]==0.10.2
equinox==0.13.8

# This may contain private dependencies via SSH URLs:
# git+ssh://git@github.com/username/repo.git@branch
# (use `tesseract build --forward-ssh-agent` to grant the builder access to your SSH keys)
../../shared_code
